<nav class="navbar navbar-default navbar-wagon" role="navigation">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to root_path, class: "navbar-brand" do %>
          <%= image_tag "dreambnb-logo.svg" %>
        <% end %>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
            <li class="dropdown">
              <%= link_to "#", {class: "dropdown-toggle", "data-toggle" => "dropdown", "role" => "button", "aria-expanded" => "false"} do %>
                <%= image_tag current_user.mini_pic, class: "img-avatar" %>
                <%= "#{current_user.first_name || current_user.email}" %><span class="caret"></span>
              <% end %>

              <ul class="dropdown-menu" role="menu">
                <li>
                  <%= link_to user_path(current_user) do %>
                    <i class="fa fa-user"></i> <%= t ".profile", default: "Profile" %>
                  <% end %>
                </li>
                <!-- <li>
                  <%= link_to "#" do %>
                    <i class="fa fa-home"></i> <%= t ".profile", default: "Flat" %>
                  <% end %>
                </li> -->
                <li>
                  <%= link_to destroy_user_session_path, method: :delete do %>
                    <i class="fa fa-sign-out"></i> <%= t ".sign_out", default: "Sign out" %>
                  <% end %>
                </li>
              </ul>
            </li>
            <!-- <li><%= link_to "Sign out", destroy_user_session_path, method: 'delete' %></li> -->
            <li><%= link_to "Add flat", new_flat_path, {class: "btn"} %></li>
          <% else %>
          <!-- some modal code here, not used but for future purposes eventually ... -->
            <li><%= link_to 'Sign up', new_user_registration_path, "data-toggle" => "modal" %></li>
            <li><%= link_to 'Sign in', new_user_session_path, "data-toggle" => "modal" %></li>
            <li><%= link_to user_omniauth_authorize_path(:facebook) do %>
              Sign in with <i class="fa fa-facebook-square"></i><% end %>
            </li>
          <% end %>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>